// Import area

import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';

// Deifination area

const root = ReactDOM.createRoot(document.querySelector('.rootElement'));
root.render(<App/>);

// Export area

